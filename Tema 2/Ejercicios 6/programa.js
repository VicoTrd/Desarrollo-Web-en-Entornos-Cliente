/*
document.getElementById("navegador").textContent = navigator.userAgent;
document.getElementById("idioma").textContent = navigator.language;
document.getElementById("plataforma").textContent = navigator.platform;
document.getElementById("resolucion").textContent = `${screen.width} x ${screen.height}`;


// URL actual
document.getElementById("url").textContent = window.location.href;


// Botón redirección
document.getElementById("btnRedirigir").addEventListener("click", () => {
window.location.href = "https://developer.mozilla.org/";
});


// Información detallada de screen
document.getElementById("screenTotal").textContent = `${screen.width} x ${screen.height}`;
document.getElementById("screenDisponible").textContent = `${screen.availWidth} x ${screen.availHeight}`;
document.getElementById("screenOrientacion").textContent = screen.orientation?.type || "No soportado";
document.getElementById("screenColor").textContent = `${screen.colorDepth} bits`;*/
