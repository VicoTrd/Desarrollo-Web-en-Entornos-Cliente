const isOdd = x => x % 2 !== 0

const inRange = (x, min, max) => x >= min && x <= max

const getBiggestNumber = numbers => Math.max(...numbers)

const getPercentage = (number, percentage) => number * (percentage / 100)